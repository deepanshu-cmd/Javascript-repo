let a = 26;
//! 'a'(variable) is going to store an integer data.
console.log(a);

a = "Babita";
//! Here data_type of variable changed from an integer to a string literal at run time.
console.log(a);

let num = 26, Name = "Babita", character = 'B';
console.log(num, '\n', Name, '\n', character);

// ^ Javascript is dynamically type language(we can change data_type of a variable at run time).Varaible is a container which contain data at a given memory location or variable is a named memory location.

// ** [let variable_name = value (or a litral)] ------> Declaration & initialization of variable in JS.

// ** let --> is a reserved keyword in JS.