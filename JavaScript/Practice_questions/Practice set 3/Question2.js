// ? W.A.P to print mean of 5 numbers.
// const prompt = require('prompt-sync')(); //! exclude this to use below code on browser.
const mean = (a, b, c, d, e) => {
    return parseFloat((a + b + c + d + e) / 5)
}

console.log(mean(26, 20, 15, 11, 6))

