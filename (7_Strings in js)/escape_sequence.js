// const prompt = require('prompt-sync')(); //! exclude this to use below code on browser.
let s = 'babi\'ta' //* backslash(\) is used to insert quotes(double or single) between string characters
console.log(s)
console.log(s.length)
let s2 = 'babita\nthakur' //* backslash n(\n) used to creat a newline and backslash t(\t) --->horizontal tab.
console.log(s2)
console.log(s2.length)