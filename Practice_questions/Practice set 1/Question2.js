// ** Creat a variable of type string and add a number in it.

let s = "Babita"
let b = 26
console.log("s+b = ", s + b)
console.log("data_type of s:", typeof s)
console.log("data_type of b:", typeof b)
console.log("data_type of s+b : ", typeof (s + b))

// ! Whenever we add somthing with string that value get concatinate with string.
// ! Always use brakets when you want to know data_type of an expression.