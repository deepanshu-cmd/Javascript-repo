// ? Tell the output of the code ----> consol.log("har\"".length)
// const prompt = require('prompt-sync')(); //! exclude this to use below code on browser.

console.log("har\"".length) // ^ Output of this code will be length of "har\""(4).